# ====== Runtime ======
# 运行环境：dev / prod
ENV=dev

# 数据库连接（示例：本地 SQLite）。也可改为 Postgres 等。
# SQLite 相对路径（建议将 DB 文件放到 data/ 目录）
DATABASE_URL=sqlite:///./data/openwebui.db

# 上传目录（相对路径或绝对路径均可）
UPLOAD_DIR=./data/uploads


# ====== Vector DB （向量数据库）======
# 已内置多个向量后端，可选：chroma / qdrant / pgvector / milvus / elasticsearch / opensearch / s3vector / weaviate / oracle23ai
VECTOR_DB=weaviate

# Weaviate 专用配置（当 VECTOR_DB=weaviate 时生效）
WEAVIATE_URL=http://localhost:8080
WEAVIATE_COLLECTION_PREFIX=open_webui


# ====== Content Extraction （内容抽取引擎）======
# 可选：docling / tika / document_intelligence（Azure）/ mistral_ocr / external / alibaba_idp（本仓库新增）
CONTENT_EXTRACTION_ENGINE=alibaba_idp

# 阿里云 DocMind 解析相关选项（根据需要调整）
ALIBABA_IDP_ENABLE_LLM=true
ALIBABA_IDP_ENABLE_FORMULA=true
ALIBABA_IDP_MAX_CHUNK_SIZE=1000
ALIBABA_IDP_CHUNK_OVERLAP=100

# 阿里云凭证（示例占位，建议使用 alibabacloud_credentials 标准机制注入）
# 如使用环境变量方式，可按需取消注释并填写
# ALIBABA_CLOUD_ACCESS_KEY_ID=your_access_key_id
# ALIBABA_CLOUD_ACCESS_KEY_SECRET=your_access_key_secret
# ALIBABA_CLOUD_REGION=cn-hangzhou


# ====== RAG / Embedding （嵌入与重排，示例）======
# 选择嵌入引擎与模型；也可改为 azure-openai / ollama 等
RAG_EMBEDDING_ENGINE=openai
RAG_EMBEDDING_MODEL=text-embedding-3-small
RAG_OPENAI_API_BASE_URL=https://api.openai.com/v1
RAG_OPENAI_API_KEY=sk-your-key

# 如使用 Azure OpenAI，可示例：
# RAG_EMBEDDING_ENGINE=azure-openai
# RAG_OPENAI_API_BASE_URL=https://<your-azure-openai>.openai.azure.com/
# RAG_OPENAI_API_KEY=your-azure-key


# ====== Misc （可选项，按需启用）======
# CORS 允许来源（如需限制可设置为具体域名，逗号分隔）
# CORS_ALLOW_ORIGINS=*

# 日志级别（info / debug / warning / error）
# LOG_LEVEL=info

# Langchain-Chatchat 缓存大小
CACHED_VS_NUM = 5
CACHED_MEMO_VS_NUM = 100
